#ifndef MAINWINDOW_H
#define MAINWINDOW_H

#include <QWidget>

#include "rdv.h"

class MainWindow : public QWidget
{
    Q_OBJECT

public:
    MainWindow(QWidget *parent = nullptr);
    ~MainWindow();

private:

private slots:
};

#endif // MAINWINDOW_H
