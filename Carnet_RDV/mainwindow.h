#ifndef MAINWINDOW_H
#define MAINWINDOW_H

#include <QWidget>

#include "rdv.h"

class MainWindow : public QWidget
{
    Q_OBJECT

private:

private slots:

public:
    MainWindow(QWidget *parent = nullptr);
    ~MainWindow();

};

#endif // MAINWINDOW_H
